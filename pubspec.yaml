name: cajero_salado
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  hive: ^2.2.3 
  hive_flutter: ^1.1.0
  get_storage: ^2.1.1
  flutter_settings_ui: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  hive_generator: 
  build_runner: 

flutter:
  uses-material-design: true
